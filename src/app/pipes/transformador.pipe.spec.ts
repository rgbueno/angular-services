import { TransformadorPipe } from './transformador.pipe';

describe('TransformadorPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformadorPipe();
    expect(pipe).toBeTruthy();
  });
});
